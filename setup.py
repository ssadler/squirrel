#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name='squirrel',
    description='Psycopg2 wrapper for tornadp',
    version='0.1',
    author='scott sadler',
    py_modules=['squirrel'],
)
